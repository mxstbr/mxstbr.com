'use client'
import React, { useEffect, useState } from 'react'
import Edit from 'react-feather/dist/icons/edit-3'

export function EditButton({ cuid }: { cuid: string }) {
  // Disabled for local MDX files - no external editor needed
  return null
}
