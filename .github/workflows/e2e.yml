name: E2E Tests

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    env:
      HASHNODE_ACCESS_TOKEN: ${{ secrets.HASHNODE_ACCESS_TOKEN }}
      GITHUB_ACCESS_TOKEN: ${{ secrets.GITHUB_ACCESS_TOKEN }}
      UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
      UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
      CAL_PASSWORD: ${{ secrets.CAL_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm test:chores

      - run: pnpm exec tsc --noEmit

      - name: Verify generated chore behavior docs
        run: |
          node scripts/render-chores-behaviors.mjs
          pnpm exec prettier --write docs/chores/inventory.md docs/chores/rebuild-features.md docs/chores/coverage.md
          git diff --exit-code -- docs/chores

      - run: pnpm exec playwright install --with-deps chromium

      - run: pnpm test:e2e
